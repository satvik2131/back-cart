from fastapi import APIRouter

# Placeholder aggregate router. Feature routers will be included here as the
# application grows, e.g. ``api_router.include_router(items.router)``.
api_router = APIRouter()
